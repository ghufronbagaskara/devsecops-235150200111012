services:
  juice-shop:
    image: bkimminich/juice-shop
    container_name: juice-shop
    ports:
      - "3000:3000"
    restart: unless-stopped
    # Catatan: aplikasi ini SENGAJA rentan — hanya untuk lab di mesin Anda sendiri.

# Fase remediasi (Minggu 12+): jalankan fork Anda dari folder app/ dengan meng-uncomment di bawah,
# lalu sesuaikan port/Dockerfile.
#  app:
#    build: ./app
#    ports:
#      - "3001:3000"
#    env_file:
#      - .env
